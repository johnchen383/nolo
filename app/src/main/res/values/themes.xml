<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Nolo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/navy</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/light_brown</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:background">@color/navy</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Bottom navigation style-->
    <style name="BottomNavigation">
        <item name="itemBackground">@drawable/nav_indicator</item>
        <item name="elevation">0dp</item>
        <item name="labelVisibilityMode">unlabeled</item>
        <item name="itemRippleColor">@android:color/transparent</item>
    </style>

    <style name="TextInputAppearance" parent="TextAppearance.AppCompat">
        <item name="colorAccent" tools:override="true">@color/colorAccent</item>
        <item name="colorControlNormal" tools:override="true">@color/colorControlNormal</item>
        <item name="colorControlActivated" tools:override="true">@color/colorControlActivated</item>
        <item name="colorControlHighlight" tools:override="true">@color/colorControlHighlight</item>
        <item name="android:textColorHint" tools:override="true">@color/white</item>
    </style>

</resources>